PREFIX : <urn:absolute/prototyp#>
PREFIX owl: <http://www.w3.org/2002/07/owl#>

	# Diese Anfrage beinhaltet nur die Anfrage nach den eingefügten Parametern. Es wird an keiner Stelle nach einem
	#  berechneten Parameter gefragt.
SELECT *
WHERE {

#* update_new
        # Kommando um neue Parameter einzufügen. Wichtig: Raute und Stern.

?p a :elektrische_Feldstaerke;
        # System erkennt, dass eine Breite eingefügt werden soll.
     :hat_Wert ?wert.
        # Werte müssen über einen FILTER eingefügt werden.  Die direkte Angabe geht nicht.
#* update_amount_values 10
  FILTER(?wert <= 35000000)
  FILTER(?wert > 39000000)

#*update_end
        # Ab hier werden keine neuen Parameter erkannt.

#* update_new
        # Kommando um neue Parameter einzufügen, hier um neues Material einzufügen

?mat :hat_Parameter ?p;
:hat_Name ?materialName.
  FILTER (?materialName IN ('TestMaterialNeu', 'Test2MaterialNeu', 'Stapelaktor_1'))
#*update_end
        # Ab hier werden wieder keine neuen Parameter bzw. Materialien erkannt.
}